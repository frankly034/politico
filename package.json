{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node --require babel-register server/server.js",
    "server": "nodemon --require babel-register server/server.js",
    "test": "nyc --reporter=text-lcov --reporter=text mocha --require babel-register server/test/**.test.js --exit || true" ,
    "coveralls": "nyc report --reporter=text-lcov npm test",
    "coverage": "nyc report --reporter=text-lcov | coveralls"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "chai": "^4.2.0",
    "coveralls": "^3.0.2",
    "eslint": "^5.12.1",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-plugin-import": "^2.16.0",
    "mocha": "^5.2.0",
    "mocha-lcov-reporter": "^1.3.0",
    "nyc": "^13.1.0",
    "supertest": "^3.4.2"
  },
  "dependencies": {
    "babel-env": "^2.4.1",
    "babel-register": "^6.26.0",
    "body-parser": "^1.18.3",
    "express": "^4.16.4"
  }
}
